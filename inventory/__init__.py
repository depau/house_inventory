from django.utils.translation import ugettext_lazy as _

# These are actually defined elsewhere but I want to provide custom translation
# Placing them somewhere makes sure makemessages picks them up
_("Log out")
_("Parent")
